
<cfcomponent>
	<cffunction name="login" access="public" returntype="void">
		<!-- check if username and password exist then check if they match -->
		<!--<cfif loginIsSuccessful>
			<cfset announceEvent("loginSucceeded", arguments.event.getArgs()) />
		<cfelse>
  			<cfset announceEvent("loginFailed", arguments.event.getArgs()) />
		</cfif>-->

	</cffunction>
	
</cfcomponent>